services:
  ai-clinical-assistant-backend:
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      ENV_TYPE: development
    command: >
      uvicorn main:app --host 0.0.0.0 --port 8000 --reload
